/**
 * CRM FRONTEND CONFIG
 *
 * NEXT STEP:
 * After we deploy the Google Apps Script backend as an API,
 * paste its /exec URL here.
 */
const CRM_CONFIG = {
  API_URL: "",
  APP_NAME: "CRM Portal"
};
